# Licencias de esta carpeta / Licences in this directory

**Los archivos de `Legacy/` NO están cubiertos por el `LICENSE` de ReaSet.**
Son referencias históricas, superadas por `Reaset.lua`, y cada una conserva la
licencia y la autoría de quien la escribió.

**Files in `Legacy/` are NOT covered by ReaSet's `LICENSE`.** They are
historical references, superseded by `Reaset.lua`, and each keeps the licence
and authorship of whoever wrote it.

| Archivo / File | Autor / Author | Licencia / Licence |
|---|---|---|
| `X-Raym_Convert Lyrics track items notes for dedicated web browser interface.lua` | X-Raym · https://www.extremraym.com | **GPL v3** |
| `X-Raym_Convert Chords track items notes for dedicated web browser interface.lua` | X-Raym · https://www.extremraym.com | **GPL v3** |
| `ReaSet_NativeLoop.lua` | ReaSet project | Ver `../LICENSE` |

## Por qué siguen acá / Why they are still here

Fueron el punto de partida conceptual del puente de letras y acordes, y se
conservan por atribución y trazabilidad. **`Reaset.lua` no deriva de ellos**:
la comparación línea a línea da 18 líneas idénticas sobre 119, y todas son
`end` o `break` — ruido sintáctico de Lua. Cero funciones compartidas.
El detalle está en `../docs/RELICENSING.md`.

They were the conceptual starting point for the lyrics and chords bridge, kept
here for attribution and traceability. **`Reaset.lua` does not derive from
them**: a line-by-line comparison finds 18 identical lines out of 119, all of
which are `end` or `break` — Lua syntax noise. Zero shared functions. Details
in `../docs/RELICENSING.md`.
